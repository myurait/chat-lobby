# Development Log

- File: `log_20260405184659.md`
- Scope: Phase B canonical publish flow
- Period: 2026-04-05

---

## Entry 1

- Date: 2026-04-05T18:46:59+09:00
- Author: myurait
- Task: Phase B の最初の到達点として、共有正本レイアウトへ Markdown を保存する publish tool を実装した。
- Changes:
  - `tools/publish_to_repo.py` を追加し、spec / ADR / worklog を canonical repo layout へ保存する CLI を実装した。
  - `docs/templates/spec.md`, `docs/templates/adr.md`, `docs/templates/worklog.md` を追加した。
  - `docs/operations/document-placement.md` を追加し、保存先と命名規約を固定した。
  - `tests/test_publish_to_repo.py` を追加し、slug 生成、保存先、レイアウト検証、重複防止をテストした。
  - README と ADR、ロードマップを更新し、CLI ベースの正本化方針を記録した。
- Verification:
  - `python3 -m unittest tests/test_publish_to_repo.py`
  - `python3 tools/publish_to_repo.py --repo workspace/templates/chatlobby-canonical --kind spec --title "Example feature spec" --body "Initial summary" --overwrite`
- Issues:
  - 現段階では Open WebUI 会話や Notes から直接呼び出していないため、Phase B は未完了である。
- Follow-up:
  - Open WebUI の tool / pipeline から `publish_to_repo.py` を呼び出す導線を追加する。
- Reusable lesson:
  - 正本化フローは UI 統合の前に、保存先・命名・テンプレートを CLI で固定すると後続統合が安定しやすい。

## Entry 2

- Date: 2026-04-05T18:46:59+09:00
- Author: myurait
- Task: 開発言語の主従と Open WebUI 依存境界を明確化した。
- Changes:
  - `decisions.md` の ADR-001 に、Open WebUI は upstream を利用し、fork / vendoring はしない前提で開発することを明記した。
  - 同 ADR に、fork / vendoring が必要な場合は実施前にユーザーの意思決定を仰ぐ条件を追加した。
  - `decisions.md` の ADR-004 を、TypeScript 主体、Python は Open WebUI 連携と補助用途に限定する内容へ修正した。
  - `design/01-project-charter.md` と `design/02-architecture.md` に同方針を反映した。
- Verification:
  - `decisions.md`, `design/01-project-charter.md`, `design/02-architecture.md` の記述整合を目視確認した。
- Issues:
  - なし
- Follow-up:
  - 将来、Open WebUI の upstream 機能だけでは要件を満たせないと判明した場合は、fork / vendoring の必要性と維持コストを整理した上で別途判断する。
- Reusable lesson:
  - 「現時点ではそうしていない」ではなく、「そうしない前提で進める」と先に固定した方が、後続の AI や実装判断のぶれを抑えやすい。

## Entry 3

- Date: 2026-04-05T18:55:24+09:00
- Author: myurait
- Task: Open WebUI の会話から canonical repo へ publish できる統合を実装し、Phase B を完了させた。
- Changes:
  - `docker-compose.yml` の `open-webui` に `workspace/`, `tools/`, `docs/` の bind mount を追加した。
  - `tools/openwebui/chatlobby_publish_pipe.py` を追加し、Open WebUI の custom `Pipe` function から `publish_to_repo.py` を呼ぶ統合を実装した。
  - `tools/sync_openwebui_publish_pipe.py` を追加し、管理 API 経由で function を登録・更新・有効化できるようにした。
  - `tests/test_publish_to_repo.py` の import 初期化を修正し、CLI テストを通る状態にした。
  - README に publish pipe の同期方法と chat からの使用方法を追記した。
  - ADR-008 を追加し、Open WebUI 統合方式を upstream function pipe + 外部 CLI に固定した。
- Verification:
  - `python3 -m unittest tests/test_publish_to_repo.py`
  - `docker compose config`
  - `OPEN_WEBUI_PORT=3001 WEBUI_URL=http://localhost:3001 docker compose up -d open-webui`
  - `python3 tools/sync_openwebui_publish_pipe.py --webui-url http://localhost:3001 --email admin@example.com --password chatlobby-admin-password`
  - `POST /api/chat/completions` with model `chatlobby_publish`
  - `/workspace/repos/publish-smoke/worklog/2026-04-05-phase-b-api-smoke.md` の生成確認
- Issues:
  - 現在の publish UX は構造化 JSON を前提にしており、自然言語からの自動整形は未実装である。
- Follow-up:
  - Phase C で Claude Code 接続の最小 adapter と起動導線を作る。
- Reusable lesson:
  - Open WebUI 側の upstream extension point を使い、重い fork を避けたまま会話起点の保存フローを先に成立させる方が、初期フェーズの速度と保守性の両方に効く。
