# Development Log

- File: `log_20260405190107.md`
- Scope: Phase C Claude adapter bootstrap
- Period: 2026-04-05

---

## Entry 1

- Date: 2026-04-05T19:01:07+09:00
- Author: myurait
- Task: Phase C の最初の到達点として、Claude Code を呼び出すローカル adapter を実装した。
- Changes:
  - `package.json` と `tsconfig.json` を追加し、依存追加なしで TypeScript ベースの service を置ける土台を作った。
  - `services/claude-adapter/src/server.ts` を追加し、`claude -p --output-format json` を呼び出すローカル HTTP adapter を実装した。
  - adapter API は `GET /health`, `GET /tasks`, `GET /tasks/:id`, `POST /tasks` の最小構成にした。
  - README に Claude adapter の起動方法と API 例を追加した。
  - ADR-009 を追加し、最初の Claude 接続方式を「ローカル CLI を包む HTTP adapter」に固定した。
- Verification:
  - `node services/claude-adapter/src/server.ts`
  - `curl http://127.0.0.1:8787/health`
  - `curl -X POST http://127.0.0.1:8787/tasks ...`
  - `curl http://127.0.0.1:8787/tasks/<task-id>`
  - 実タスクが `succeeded` となり、`result.result == "ok"` を確認
- Issues:
  - まだ Open WebUI から直接叩いていないため、Phase C の task 実行 UI と chat 返却は未完了である。
- Follow-up:
  - Open WebUI 側から Claude adapter の `POST /tasks` を呼ぶ導線を設計する。
  - task 状態の frontdoor 返却方法を決める。
- Reusable lesson:
  - まず CLI を薄い HTTP adapter で包んで task 境界を固定すると、frontdoor 統合前でもワーカー実行面を切り出して検証しやすい。

## Entry 2

- Date: 2026-04-05T19:06:12+09:00
- Author: myurait
- Task: Open WebUI の前面チャットから Claude adapter を起動し、結果返却まで通して Phase C を完了させた。
- Changes:
  - `tools/openwebui/chatlobby_claude_task_pipe.py` を追加し、Open WebUI custom `Pipe` function から Claude adapter API を呼ぶ統合を実装した。
  - `tools/openwebui_sync.py` を追加し、Open WebUI function 同期処理を共通化した。
  - `tools/sync_openwebui_claude_pipe.py` を追加し、`chatlobby_claude_task` の登録・更新・有効化を自動化した。
  - README に Claude pipe の同期方法と、Open WebUI からの task 実行例を追記した。
  - ADR-010 を追加し、Phase C の frontdoor 統合方式を固定した。
- Verification:
  - `CLAUDE_ADAPTER_HOST=0.0.0.0 node services/claude-adapter/src/server.ts`
  - `python3 tools/sync_openwebui_claude_pipe.py --webui-url http://localhost:3001 --email admin@example.com --password chatlobby-admin-password`
  - Open WebUI コンテナ内から `http://host.docker.internal:8787/health` 到達確認
  - `POST /api/chat/completions` with model `chatlobby_claude_task`
  - 会話応答で `Claude task succeeded` と `result: ok` を確認
- Issues:
  - 現在の task 実行 UI は構造化 JSON を送る形であり、専用 UI や継続的な状態表示は未実装である。
- Follow-up:
  - Phase D で Codex 側にも同様の adapter 境界を作る。
  - 将来の status panel で Claude / Codex の task 状態を共通表示できるようにする。
- Reusable lesson:
  - upstream extension point と host sidecar を組み合わせれば、frontdoor 統合と worker 実行を分離したまま段階的に完成度を上げられる。
