# Development Log

- File: `log_20260405174901.md`
- Scope: Phase A bootstrap
- Period: 2026-04-05

---

## Entry 1

- Date: 2026-04-05T17:49:01+09:00
- Author: myurait
- Task: Phase A の最初の未完了項目として `docker-compose.yml` を作成し、初期起動手順を整備した。
- Changes:
  - ルートに `docker-compose.yml` を追加し、`open-webui`、`open-terminal`、`ollama` の 3 サービスを定義した。
  - `.env.example` を追加し、管理者アカウント、Open WebUI secret、Open Terminal API key の入力点を固定した。
  - `README.md` と `README.ja.md` を更新し、起動手順、モデル追加手順、Compose 検証手順を明記した。
  - `decisions.md` に Phase A の Open Terminal 配線方針を ADR として追加した。
  - `roadmap/01-initial-roadmap.md` の `docker-compose.yml` 項目を完了に更新した。
- Verification:
  - `docker compose config` による Compose 定義の静的検証を予定。
- Issues:
  - Open WebUI の PersistentConfig は初回値を内部保存するため、初回起動後の設定変更には Admin UI またはボリューム初期化の理解が必要。
  - 実起動とスマホ接続確認は次タスクに残る。
- Follow-up:
  - 認証の実起動確認。
  - Open Terminal と File Browser の UI 上での有効化確認。
  - スマホブラウザからの疎通確認。
- Reusable lesson:
  - 初回ブートで固定される Open WebUI 設定は、README と knowledge に巻き取っておかないと再現性を損なう。

## Entry 2

- Date: 2026-04-05T17:49:01+09:00
- Author: myurait
- Task: `reference/historical-documents` の内容が現行 `development-docs` に適切に展開されているか監査し、未展開項目を正規文書へ移した。
- Changes:
  - `reference/README.md` に `reference/historical-documents` が歴史的資料であり、通常の作業導線では参照不要であることを明記した。
  - `design/01-project-charter.md` に会話体験、知識共有、プロジェクト文脈、自動振り分けに関する中核要件を明記した。
  - `design/02-architecture.md` に運用境界、ワーカー役割、状態可視化方針、目標リポジトリ構成を追加した。
  - `roadmap/01-initial-roadmap.md` にフェーズ方針、各フェーズの目的、初期優先順位、主要リスクと対策を追加した。
- Verification:
  - 原典 3 文書の章立てと現行文書の配置先を突き合わせ、現行文書だけで方針・段階計画・リスクが追えることを確認した。
- Issues:
  - 将来、`docs/operations/` や `docs/templates/` など実体化した時点で、目標構成の記述も実体に合わせて細分化する必要がある。
- Follow-up:
  - 新規ドキュメントが増えたら、同じ情報を roadmap / architecture / decisions に重複保持しすぎないよう再整理する。
- Reusable lesson:
  - 原典の保存自体は有用だが、現行の正本が原典依存のままだと後続 AI の読み取りコストと判断揺れが増える。早い段階で現行体系へ吸収すべきである。

## Entry 3

- Date: 2026-04-05T17:49:01+09:00
- Author: myurait
- Task: root リポジトリと `development-docs` リポジトリの現在変更を、監査ログ付きで commit / push する準備を整えた。
- Changes:
  - Phase A bootstrap と文書正規化の作業内容を、この development log に時系列で残した。
  - root 側は Compose 起動基盤と README 更新、`development-docs` 側は設計・ロードマップ・レビュー証跡の更新として切り分けた。
- Verification:
  - root: `docker compose config`
  - development-docs: 文書間の整合と review evidence の存在確認
- Issues:
  - 実起動確認は前回ターンで途中停止しており、今回の commit 対象には含めない。
- Follow-up:
  - commit / push 後に `reference` 配下へテンプレートを clone し、履歴参照と正本参照の位置づけを追加で整備する。
- Reusable lesson:
  - nested repository を採用している場合、成果物ごとに commit 境界を分けると履歴の意味が崩れにくい。

## Entry 4

- Date: 2026-04-05T17:50:37+09:00
- Author: myurait
- Task: `reference` に開発ドキュメンテーションルールの正本参照元を接続し、ローカル参照 clone を Git 管理から除外した。
- Changes:
  - `reference/my-project-template/` に `https://github.com/myurait/my-project-template` を shallow clone した。
  - `reference/README.md` に、この clone が開発ドキュメンテーションルールの正本参照元であることを記載した。
  - `.gitignore` を追加し、ローカル clone を `development-docs` リポジトリの管理対象外にした。
  - `roadmap/01-initial-roadmap.md` に文書正規化メモとして反映した。
- Verification:
  - clone 後に `development-docs/.gitignore` で `reference/my-project-template/` が ignore 対象になっていることを確認する。
- Issues:
  - 正本参照元は外部リポジトリであるため、更新追従のルールは今後必要になる。
- Follow-up:
  - 必要になった時だけテンプレート repo を pull し、差分のうち採用すべきルールを現行文書へ反映する。
- Reusable lesson:
  - 正本参照元をローカル clone で近くに置く場合でも、作業リポジトリの履歴には直接混ぜず、参照と展開を明示的に分離した方が保守しやすい。

## Entry 5

- Date: 2026-04-05T18:04:23+09:00
- Author: myurait
- Task: 文書粒度ルールを明文化し、現行文書がそのルールに従っているか監査した。
- Changes:
  - `knowledge.md` の Documentation Discipline に、「文書は適切な粒度で分割し、追記で肥大化させない」規約を追加した。
  - `rules/coding-conventions.md` を監査し、レビュー・再レビュー・文書規約・開発フローまで抱えていたため責務過多と判断した。
  - process 系規約を新規 `rules/development-process.md` へ切り出し、`rules/coding-conventions.md` を coding rule 中心へ整理した。
  - `INDEX.md` と `AI_KNOWLEDGE.md` の読書順と関係図を更新した。
- Verification:
  - `rules/coding-conventions.md` は 368 行から 196 行へ縮小し、`rules/development-process.md` は 175 行の独立文書として分離された。
  - 監査時点で他の maintained document はおおむね 130 行以下で、直ちに再分割を要する肥大化は見当たらなかった。
- Issues:
  - 将来的に `rules/development-process.md` が再び肥大化した場合は、review policy と development flow を別文書へ分割する余地がある。
- Follow-up:
  - 今後、新規ルール追加時は既存 overview 文書への追記ではなく、専用文書化の要否を先に判定する。
- Reusable lesson:
  - 文書粒度ルールは宣言だけでは不十分で、既存文書の責務監査とセットで適用しないと運用が形骸化する。

## Entry 6

- Date: 2026-04-05T18:04:23+09:00
- Author: myurait
- Task: 原則不変の規約文書を `rules/` 配下へ分離し、通常の設計・決定・ロードマップ文書と混在しない構造へ再編した。
- Changes:
  - `rules/AI_RUNTIME_RULES.md`, `rules/coding-conventions.md`, `rules/development-process.md`, `rules/testing.md` を規約文書群として `rules/` 配下へ集約した。
  - `rules/README.md` を追加し、`rules/` 配下の文書は明示的なユーザー指示なしに変更しないことを明文化した。
  - `knowledge.md` に、原則不変の規約文書は `rules/` に置くという運用ルールを追加した。
  - `INDEX.md`, `AI_KNOWLEDGE.md`, `README`, `AGENT.md`, `CLAUDE.md` などの参照導線を `rules/...` へ更新した。
- Verification:
  - `rules/` 配下に規約文書が集約され、意思決定や計画更新で変わる文書群と分離されたことを確認した。
  - 旧パス参照を検索し、現行導線で使う参照先を更新した。
- Issues:
  - 歴史ログや過去レビューの本文には旧ファイル名を引用した記述が残ることがあるが、これは履歴文脈として許容する。
- Follow-up:
  - 今後、新しい不変ルール文書を追加する場合は最初から `rules/` 配下に置く。
- Reusable lesson:
  - 規約文書と進行中の設計・判断文書を物理的に分離すると、「何を守る文書か」と「何を更新してよい文書か」の境界が明確になる。

## Entry 7

- Date: 2026-04-05T18:10:10+09:00
- Author: myurait
- Task: Phase A の未完了項目として、Open Terminal の専用 workspace と共有 Git リポジトリ初期構成を固定した。
- Changes:
  - `docker-compose.yml` の `open-terminal` に `./workspace:/workspace` bind mount を追加した。
  - `workspace/README.md` と `workspace/.gitignore` を追加し、runtime clone 領域と template 領域を分離した。
  - `workspace/templates/chatlobby-canonical/` に `docs/`, `specs/`, `decisions/`, `worklog/`, `src/` を持つ共有正本テンプレートを追加した。
  - root README と ADR、ロードマップを更新し、この workspace が Phase A の専用作業領域であることを明記した。
- Verification:
  - `docker compose config` により、`open-terminal` へ `./workspace` mount が反映されることを確認する。
  - 生成物のうち `workspace/repos/` が root リポジトリの管理対象外であることを確認する。
- Issues:
  - File Browser とスマホ接続の実 UI 確認はまだ残る。
  - 認証有効化の実起動確認も未完了である。
- Follow-up:
  - Open WebUI を起動し、Open Terminal から `/workspace/repos` と template を閲覧できるか確認する。
  - その確認が取れたら File Browser とスマホ接続確認を進める。
- Reusable lesson:
  - terminal の隔離を「コンテナ化した」だけでなく、「どの host path を触らせるか」まで固定して初めて運用上の境界が明確になる。

## Entry 8

- Date: 2026-04-05T18:28:54+09:00
- Author: myurait
- Task: ローカル環境へのインストール系操作を行う前には必ずユーザーへ通知するルールを runtime rules に追加した。
- Changes:
  - `rules/AI_RUNTIME_RULES.md` の Tier 2 に、ソフトウェア、パッケージ、依存関係のインストールやローカル環境変更前の通知義務を追加した。
- Verification:
  - runtime rules の Tier 2 に、対象操作と通知タイミングが明示されていることを確認した。
- Issues:
  - None.
- Follow-up:
  - 今後の実行では、ユーザー環境に変更を加える前にこのルールを明示的に適用する。
- Reusable lesson:
  - approval の有無とは別に、ユーザー環境変更は事前通知をルール化しておくと運用の期待値ずれを防げる。

## Entry 9

- Date: 2026-04-05T18:28:54+09:00
- Author: myurait
- Task: Phase A の認証有効化と File Browser 成立を実起動で確認した。
- Changes:
  - `.env` をローカル作業用に配置し、`open-webui` を起動した。
  - `signin` API で admin login 成功、`signup` API で public sign-up 拒否を確認した。
  - Open WebUI から `configs/terminal_servers`, `terminals/`, `terminal_servers/verify` を呼び、terminal connection が認識・検証されることを確認した。
  - terminal proxy 経由で `/workspace` の `/files/list` を呼び、`repos/` と `templates/` が列挙されることを確認した。
  - README に、ポート競合時の `OPEN_WEBUI_PORT` 切替と手動確認手順を追記した。
- Verification:
  - `curl -X POST /api/v1/auths/signin` が成功。
  - `curl -X POST /api/v1/auths/signup` が権限拒否。
  - `GET /api/v1/configs/terminal_servers` と `POST /api/v1/configs/terminal_servers/verify` が成功。
  - `GET /api/v1/terminals/chatlobby-docker-terminal/files/list?directory=/workspace` が成功。
- Issues:
  - このホストでは `3000` 番ポートが別コンテナに使用されていたため、確認時は `3001` で起動した。
  - スマホからの実機接続確認はユーザー環境依存のため未実施。
- Follow-up:
  - ユーザーに同一 LAN 上のスマホから接続してもらい、最終項目を閉じる。
- Reusable lesson:
  - UI を直接操作しなくても、auth API と terminal proxy を組み合わせれば frontdoor と背後 terminal の成立条件をかなりの精度で確認できる。

## Entry 10

- Date: 2026-04-05T18:32:31+09:00
- Author: myurait
- Task: 実機スマホからのログイン確認をもって Phase A を完了状態に更新した。
- Changes:
  - ユーザーからスマホ実機ログイン成功の確認を受け、`roadmap/01-initial-roadmap.md` の `スマホからの接続確認` を完了に更新した。
  - `Current Phase` を Phase B に切り替え、次タスクを `publish tool の作成` と `Markdown テンプレート整備` に設定した。
- Verification:
  - 実機スマホログイン OK のユーザー確認を取得。
- Issues:
  - None.
- Follow-up:
  - Phase B の正本化フローへ着手する。
- Reusable lesson:
  - LAN 内の実機確認は自動化しにくいが、最後の 1 項目として切り出しておくと開発サイクルを止めにくい。
